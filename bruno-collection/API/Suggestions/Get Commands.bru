meta {
  name: Get Commands
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/suggestions/{{projectId}}/commands
  body: none
  auth: inherit
}

docs {
  # Get Commands
  
  Get slash commands.
  
  ## Path Parameters
  - `projectId` (string): Base64-encoded project path
  
  ## Response
  ```json
  {
    "suggestions": [
      {
        "type": "command",
        "name": "/edit",
        "description": "Edit a file"
      }
    ],
    "total": 5
  }
  ```
}
