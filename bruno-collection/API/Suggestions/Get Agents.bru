meta {
  name: Get Agents
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/suggestions/{{projectId}}/agents
  body: none
  auth: inherit
}

docs {
  # Get Agents
  
  Get agents (project + user level).
  
  ## Path Parameters
  - `projectId` (string): Base64-encoded project path
  
  ## Response
  ```json
  {
    "suggestions": [
      {
        "type": "agent",
        "name": "code-reviewer",
        "description": "Reviews code for best practices"
      }
    ],
    "total": 2
  }
  ```
}
