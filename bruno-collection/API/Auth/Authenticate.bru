meta {
  name: Authenticate
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth
  body: json
  auth: none
}

body:json {
  {
    "token": "{{authToken}}"
  }
}

docs {
  # Authenticate
  
  Validate an authentication token. This endpoint does not require prior authentication.
  
  ## Request Body
  - `token` (string): The authentication token to validate
  
  ## Response
  ```json
  {
    "success": true,
    "message": "Authenticated"
  }
  ```
}
