# Cursor Mobile Access Server Configuration

# Port to run the server on (default: 3847)
PORT=3847

# Authentication token (a random UUID will be generated if not set)
# AUTH_TOKEN=your-secure-token-here
