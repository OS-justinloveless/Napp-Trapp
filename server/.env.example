# Cursor Mobile Access Server Configuration

# Port to run the server on (default: 3847)
PORT=3847

# Authentication token (optional - overrides the persisted token)
# By default, the server generates a token on first run and persists it to disk.
# This means clients don't need to re-scan the QR code after server restarts.
# Only set this if you want to use a specific token (e.g., for security rotation).
# AUTH_TOKEN=your-secure-token-here

# Mobile Chat Store Retention Settings
# These settings prevent the mobile-chats.json file from growing indefinitely

# Number of days to keep conversations (default: 30)
# Conversations older than this will be automatically deleted
MOBILE_CHAT_RETENTION_DAYS=30

# Maximum number of conversations to keep (default: 100)
# If exceeded, oldest conversations will be deleted
MOBILE_CHAT_MAX_CONVERSATIONS=100
